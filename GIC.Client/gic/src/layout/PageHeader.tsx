import { PageHeader as AntdPageHeader, PageHeaderProps } from '@ant-design/pro-layout';

const PageHeader = (props: PageHeaderProps) => <AntdPageHeader {...props} />;

export default PageHeader;
